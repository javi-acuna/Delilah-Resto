const db_user = 'root';
const db_url = 'localhost';
const db_port = '3306';
const db_name = 'delilah_resto';

module.exports = {
    user: db_user,
    url: db_url,
    port: db_port,
    name: db_name
}